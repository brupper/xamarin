<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/dotnet/2021/maui/design"
    mc:Ignorable="d"
    x:Name="This"
    x:Class="Brupper.Maui.Views.ChipView">

    <Frame HasShadow="False" BorderColor="{Binding BorderColor, Source={x:Reference This}}" CornerRadius="16" HeightRequest="32" BackgroundColor="White" Padding="8,0" Margin="6">
        <StackLayout Orientation="Horizontal" IsClippedToBounds="True" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Label Text="{Binding KeyText, Source={x:Reference This}}" TextColor="{Binding BorderColor, Source={x:Reference This}}" FontSize="Small" HorizontalTextAlignment="Center" HorizontalOptions="Center" VerticalOptions="Center" VerticalTextAlignment="Center" />
            <Label Text="{Binding ValueText, Source={x:Reference This}}" TextColor="{Binding TextColor, Source={x:Reference This}}" FontSize="Small" FontAttributes="Bold" HorizontalTextAlignment="Center" HorizontalOptions="Center" VerticalOptions="Center" VerticalTextAlignment="Center" />
        </StackLayout>
    </Frame>

</ContentView>